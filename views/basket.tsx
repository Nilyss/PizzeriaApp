import React from 'react'
import { Text, View } from 'react-native'

interface BasketProps {}
export const Basket: React.FunctionComponent<BasketProps> = ({}) => {
  return (
    <View>
      <Text>basket component works</Text>
    </View>
  )
}
